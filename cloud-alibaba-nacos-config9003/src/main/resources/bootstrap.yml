server:
  port: 9003

spring:
  application:
    name: cloud-nacos-config-client
  cloud:
    nacos:
      discovery:
        # 注册中心地址
        server-addr: localhost:8848
      config:
        # 注册中心地址
        server-addr: localhost:8848
        # 文件类型
        file-extension: yaml
        #指定分组获取
        group: test
        # 指定namespace:值为namespace的id
        namespace: 4aebcd3f-c6d8-4dfa-a71a-820da35c3bca

