server:
  port: 80

spring:
  application:
    name: consumer-consul-order80
  cloud:
    #注册中心配置
    consul:
      host: 132.232.77.49
      port: 8500
      discovery:
        heartbeat:
          enabled: true
        service-name: ${spring.application.name}

